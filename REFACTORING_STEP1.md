# 🔄 단계 1: RootViewController 생성 및 AppDelegate 수정

## 완료 사항

1. ✅ RootViewController.swift 생성
   - 카메라 컨테이너 뷰
   - FlutterViewController를 자식으로 추가
   - Flutter 뷰 배경 투명 처리

## 다음 단계

다음은 AppDelegate를 수정하여:
- FlutterViewController를 생성
- RootViewController로 래핑
- window.rootViewController로 설정

단, Main.storyboard를 사용하지 않도록 설정해야 합니다.
